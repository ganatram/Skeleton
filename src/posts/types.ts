export type PostData = {
  id: number;
  title: string;
  description: string;
};

export type NewPostsData = {
  title: string;
  description: string;
};

export type SavedPostData = {
  id: number;
};
